apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fantasy-exporter-pvc
  namespace: {{ .Release.Namespace }}

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClassName }}
  resources:
    requests:
      storage: {{ .Values.storage.size }}
